# 微信 iLink 连接库

这是可被其他 Go 项目引用的基础依赖，只负责建立微信 iLink 二维码连接，不提供可执行入口，不负责模型配置、模型推理或 HTTP 监听。

目录约定：

- 根目录使用 `package ilink` 提供稳定的公共 HTTP `Handler` 构造接口。
- `internal/config` 负责只从指定 `.env` 文件加载 iLink 配置，不读取系统环境变量。
- `internal/ilink` 负责 iLink 协议请求和登录会话状态，禁止被仓库外部直接依赖。
- 不新建与仓库单一职责同名的公共子包，避免重复导入路径。
- 新增公开能力前先确认它属于“建立微信连接”职责，否则放到上层应用。

验证命令：

```bash
go fmt ./...
go vet ./...
go test ./...
```

`bot token` 和二维码凭据只允许保存在内存中，禁止写入日志、响应和提交记录。真实 `.env` 禁止提交。
